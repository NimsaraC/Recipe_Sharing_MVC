@model Recipe.Models.UserDash

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-center mt-5">User Details</h1>
<hr />

<div class="container mt-4">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">User Information</h4>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-3">Name:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.User.Name)</dd>

                        <dt class="col-sm-3">Email:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.User.Email)</dd>

                        <dt class="col-sm-3">Bio:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.User.Bio)</dd>

                        <dt class="col-sm-3">Phone:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.User.Phone)</dd>

                        <dt class="col-sm-3">Active:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.User.IsActive)</dd>
                    </dl>
                    <div class="text-center">
                        <a asp-action="Edit" asp-route-id="@Model?.User.Id" class="btn btn-primary">Edit</a>
                        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                        <a asp-controller="RecipeMs" asp-action="Create" class="btn btn-success">Add Recipe</a> <!-- Add this button -->
                        <a asp-action="Logout" class="btn btn-danger">Logout</a> <!-- Add logout link -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    <hr />

    <div class="row mt-4">
        <div class="col-12">
            <div class="section-heading">
                <h3>The Best Recipes</h3>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        @foreach (var item in Model?.recipeMs ?? Enumerable.Empty<RecipeM>())
        {
            <div class="col-12 col-sm-6 col-lg-4">
                <div class="card mb-4">
                    <img src="img/bg-img/r1.jpg" class="card-img-top" alt="">
                    <div class="card-body">
                        <h5 class="card-title">@item.Title</h5>
                        <div class="card-text">
                            <a asp-controller="RecipeMs" asp-action="DetailsUser" asp-route-id="@item.Id" class="btn btn-primary">Details</a>
                        </div>
                        <div class="ratings mt-2">
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star-o" aria-hidden="true"></i>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
